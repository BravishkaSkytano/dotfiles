- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.shortcuts:
      glob: true
      path: termux-shortcuts/*

- shell:
  - chmod 700 -R ~/.shortcuts
  - chmod 700 -R ~/.shortcuts/tasks

